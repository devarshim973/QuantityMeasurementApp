server:
  port: 8081

spring:
  application:
    name: security-service

  # MySQL Database
  datasource:
    url: jdbc:mysql://localhost:3306/security_db
    username: root
    password: Dev#@20021
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  # GitHub OAuth2
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23liWSwkjj0RtZp3PD
            client-secret: 4f045028b27f3db7ab60c87496e9007c5c494ad8
            scope: user:email

# JWT Config
jwt:
  secret: 5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
  expiration: 86400000

# Eureka Client
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health, info